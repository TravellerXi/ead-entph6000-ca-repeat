apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.userService.name }}
  labels:
    {{- include "ead.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.userService.name }}
spec:
  ports:
    - name: http
      port: {{ .Values.userService.port }}
      targetPort: http
  selector:
    app.kubernetes.io/component: {{ .Values.userService.name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.userService.name }}
  labels:
    {{- include "ead.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.userService.name }}
  annotations:
    kubernetes.io/change-cause: {{ printf "helm release %s image tag %s" .Release.Name .Values.global.imageTag | quote }}
spec:
  replicas: {{ .Values.userService.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: {{ .Values.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.rollingUpdate.maxUnavailable }}
  selector:
    matchLabels:
      app.kubernetes.io/component: {{ .Values.userService.name }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        {{- include "ead.labels" . | nindent 8 }}
        app.kubernetes.io/component: {{ .Values.userService.name }}
    spec:
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      containers:
        - name: {{ .Values.userService.name }}
          image: {{ include "ead.image" (list . .Values.userService.image) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.userService.port }}
          env:
            {{- include "ead.springEnv" . | nindent 12 }}
            - name: MONGODB_URI
              value: {{ include "ead.mongoUri" (list . .Values.userService.database) | quote }}
            - name: INSTANCE_COLOUR
              value: {{ .Values.activeColour | quote }}
          {{- include "ead.springProbes" . | nindent 10 }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.userService.resources | nindent 12 }}
          volumeMounts:
            {{- include "ead.tmpVolumeMount" . | nindent 12 }}
      volumes:
        {{- include "ead.tmpVolume" . | nindent 8 }}
---
{{- if .Values.userService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.userService.name }}
  labels:
    {{- include "ead.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.userService.name }}
  minReplicas: {{ .Values.userService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.userService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.userService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
